<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper" >
<!--    主键自增后需要回填-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (id, name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        values (#{id}, #{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="update" useGeneratedKeys="true" keyProperty="id" parameterType="com.sky.entity.DishPO">
        update dish
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="image!=null">
                image = #{image},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteByIds" parameterType="java.lang.Long">
        delete from dish where id in
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </delete>

    <select id="page" resultType="com.sky.vo.DishVO" parameterType="java.lang.String">
        select dish.*, category.name as categoryName from dish
        join category on dish.category_id = category.id
        <where>
            <if test="name != null">
                dish.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and dish.status = #{status}
            </if>
            <if test="categoryId != null">
                and dish.category_id = #{categoryId}
            </if>
        </where>
    </select>
</mapper>
